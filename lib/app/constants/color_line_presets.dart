import 'package:flutter/material.dart';

/// 默认的色线颜色（纯红）。
const Color kDefaultColorLineColor = Color(0xFFFF0000);

/// 取色卡片中“色线”所使用的固定颜色预设。
const List<Color> kColorLinePresets = <Color>[
  kDefaultColorLineColor,
  Color(0xFF0000FF), // 纯蓝
  Color(0xFF00FF00), // 纯绿
];
